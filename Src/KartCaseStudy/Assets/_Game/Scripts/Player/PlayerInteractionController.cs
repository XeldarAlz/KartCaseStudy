using Game.Interaction;
using Game.Managers;
using Game.Settings;
using UnityEngine;
using Zenject;


namespace Game.Player
{
    /// <summary>
    /// Controls the player's interaction with interactable objects in the game environment.
    /// It detects and highlights interactable objects within the player's vicinity and manages the interaction inputs.
    /// Responsible for raycasting to find interactable objects and triggering interaction events.
    /// </summary>
    public class PlayerInteractionController : MonoBehaviour
    {

    }

}
